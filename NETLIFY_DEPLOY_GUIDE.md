# 🚀 Netlify 배포 가이드

## 📋 준비사항

- ✅ GitHub 계정
- ✅ Netlify 계정 (무료)
- ✅ 프로젝트가 GitHub에 업로드되어 있어야 함

---

## 🎯 방법 1: Netlify 웹사이트에서 배포 (가장 쉬움!)

### 1단계: Netlify 가입 및 로그인

1. **Netlify 웹사이트 접속**
   ```
   https://www.netlify.com
   ```

2. **Sign up** 클릭 (또는 이미 계정이 있다면 Log in)

3. **GitHub으로 로그인** 선택
   - "Authorize Netlify" 클릭하여 권한 부여

---

### 2단계: 새 사이트 생성

1. Netlify 대시보드에서 **"Add new site"** 버튼 클릭

2. **"Import an existing project"** 선택

3. **"Deploy with GitHub"** 클릭

4. **저장소 선택**
   - "nagashino2014/Lotto" 저장소 찾기
   - 클릭하여 선택

---

### 3단계: 빌드 설정

다음과 같이 입력:

```
Site name: (원하는 이름, 예: my-lotto-app)
  └─ 나중에 변경 가능

Branch to deploy: main

Build command: (비워둠)

Publish directory: .
```

**중요**: 
- Build command는 **비워두세요** (정적 HTML이므로 빌드 불필요)
- Publish directory는 `.` (현재 디렉토리)

---

### 4단계: 배포 시작!

1. **"Deploy site"** 버튼 클릭

2. 배포 진행 상황 확인
   - 보통 1-2분 소요
   - "Site is live" 메시지가 나타날 때까지 대기

3. **배포 완료!** 🎉

---

### 5단계: 사이트 접속

배포가 완료되면 다음과 같은 URL을 받게 됩니다:

```
https://random-name-12345.netlify.app
```

**전체 URL 예시:**
```
https://random-name-12345.netlify.app/enhanced_lotto.html
```

이 URL을 브라우저에서 열면 **CORS 문제 없이 완벽하게 작동**합니다!

---

### 6단계: 사이트 이름 변경 (선택사항)

1. Netlify 대시보드에서 사이트 클릭

2. **"Site settings"** 클릭

3. **"Change site name"** 클릭

4. 원하는 이름 입력 (예: `my-lotto`)

5. 저장

**새 URL:**
```
https://my-lotto.netlify.app/enhanced_lotto.html
```

---

## 🎯 방법 2: Netlify CLI로 배포 (개발자용)

### 1단계: Node.js 설치 확인

터미널에서:
```bash
node --version
```

없다면 https://nodejs.org 에서 다운로드

---

### 2단계: Netlify CLI 설치

```bash
npm install -g netlify-cli
```

---

### 3단계: Netlify 로그인

```bash
netlify login
```

브라우저가 열리면 로그인 및 권한 부여

---

### 4단계: 프로젝트 폴더로 이동

```bash
cd C:\Users\nagas\OneDrive\문서\CodingProject\로또\Lotto
```

---

### 5단계: Netlify 초기화

```bash
netlify init
```

다음 질문에 답변:
```
? What would you like to do? 
  → Create & configure a new site

? Team:
  → (본인 팀 선택)

? Site name (optional):
  → my-lotto (원하는 이름 입력)

? Your build command:
  → (Enter - 비워둠)

? Directory to deploy:
  → . (현재 디렉토리)

? Netlify functions folder:
  → netlify/functions
```

---

### 6단계: 배포!

```bash
netlify deploy --prod
```

배포 완료 후 URL이 표시됩니다!

---

## 🔧 자동 배포 설정 (권장!)

GitHub에 코드를 푸시할 때마다 자동으로 Netlify가 배포되도록 설정:

### 이미 완료됨! ✅

방법 1로 배포했다면 **이미 자동 배포가 설정**되어 있습니다!

**작동 방식:**
```
1. 로컬에서 코드 수정
2. git add . && git commit -m "수정 내용"
3. git push origin main
4. Netlify가 자동으로 감지
5. 자동 배포 시작
6. 1-2분 후 사이트 업데이트 완료!
```

---

## 📊 배포 후 확인 사항

### 1. Functions 작동 확인

Netlify Functions가 제대로 작동하는지 확인:

```
https://your-site.netlify.app/.netlify/functions/lotto-api?round=1100
```

**정상 응답 예시:**
```json
{
  "round": 1100,
  "date": "2024-03-16",
  "numbers": [6, 15, 20, 27, 40, 42],
  "bonus": 1,
  "prize": {...}
}
```

---

### 2. 전체 앱 테스트

1. `https://your-site.netlify.app/enhanced_lotto.html` 접속

2. **당첨 체크** 탭으로 이동

3. 회차 입력: **1100**

4. **"🔍 당첨 확인"** 클릭

5. ✅ **실제 당첨번호가 표시되면 성공!**

---

## 🎨 커스텀 도메인 연결 (선택사항)

자신의 도메인이 있다면 연결할 수 있습니다:

### 1. 도메인 구매
- Namecheap, GoDaddy 등에서 구매
- 예: `mylotto.com`

### 2. Netlify에서 도메인 추가
1. Site settings → Domain management
2. "Add custom domain" 클릭
3. 도메인 입력 및 DNS 설정 따라하기

### 3. HTTPS 자동 적용
Netlify가 자동으로 Let's Encrypt SSL 인증서 발급!

---

## 📱 PWA (Progressive Web App) 만들기 (선택사항)

홈 화면에 추가할 수 있는 앱으로 만들기:

### manifest.json 파일 생성

```json
{
  "name": "로또 번호 생성기",
  "short_name": "로또앱",
  "description": "스마트한 로또 번호 생성 및 당첨 확인",
  "start_url": "/enhanced_lotto.html",
  "display": "standalone",
  "background_color": "#f5f7fa",
  "theme_color": "#4F46E5",
  "icons": [
    {
      "src": "/icon-192.png",
      "sizes": "192x192",
      "type": "image/png"
    },
    {
      "src": "/icon-512.png",
      "sizes": "512x512",
      "type": "image/png"
    }
  ]
}
```

---

## 🐛 문제 해결

### 배포는 성공했지만 Functions가 작동하지 않음

**확인사항:**
1. `netlify.toml` 파일이 있는지 확인
2. `netlify/functions/lotto-api.js` 파일 위치 확인
3. Netlify 대시보드 → Functions 탭에서 Functions 목록 확인

**해결:**
```bash
git add netlify.toml netlify/functions/lotto-api.js
git commit -m "Add Netlify Functions"
git push origin main
```

---

### "Function not found" 오류

**원인:** Functions 폴더 경로가 잘못됨

**해결:**
`netlify.toml` 파일 확인:
```toml
[build]
  functions = "netlify/functions"
```

---

### 배포 후 변경사항이 반영되지 않음

**해결:**
1. Netlify 대시보드 → Deploys 탭
2. "Trigger deploy" → "Clear cache and deploy site"

---

## 📊 Netlify 무료 플랜 제한

무료 플랜에서도 충분히 사용 가능합니다:

| 항목 | 무료 플랜 | 충분한가? |
|------|----------|----------|
| 대역폭 | 100GB/월 | ✅ 넉넉함 |
| 빌드 시간 | 300분/월 | ✅ 충분함 |
| Functions 호출 | 125K/월 | ✅ 충분함 |
| Functions 실행 시간 | 100시간/월 | ✅ 충분함 |
| 사이트 수 | 무제한 | ✅ 좋음! |

**결론:** 개인 사용에는 무료 플랜으로 충분합니다! 🎉

---

## 🎉 배포 완료!

이제 다음을 할 수 있습니다:

- ✅ 어디서든 앱에 접속 가능
- ✅ CORS 문제 완전 해결
- ✅ HTTPS로 안전한 연결
- ✅ GitHub 푸시 시 자동 배포
- ✅ 빠른 속도 (CDN 사용)

**최종 URL:**
```
https://your-site-name.netlify.app/enhanced_lotto.html
```

이 URL을 북마크하거나 친구들과 공유하세요! 🚀

---

## 💡 다음 단계

### 1. 모바일 최적화
이미 반응형 디자인이 적용되어 있습니다!

### 2. 소셜 공유
친구들에게 URL 공유:
```
https://your-site.netlify.app/enhanced_lotto.html
```

### 3. 지속적인 개선
- 기능 추가
- GitHub에 푸시
- 자동으로 배포됨!

---

**축하합니다! Netlify 배포가 완료되었습니다!** 🎊

문제가 발생하면 언제든 물어보세요! 😊

